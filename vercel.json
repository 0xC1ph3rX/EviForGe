{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "python -m pip install -r requirements-vercel.txt",
  "functions": {
    "index.py": {
      "includeFiles": "src/eviforge/api/{templates,static}/**,rules/yara/**",
      "excludeFiles": "{tests/**,.github/**,docker/**,scripts/**,demo.py,verify_expansion.py,docker-compose*.yml,Caddyfile}"
    }
  }
}
